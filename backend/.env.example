# Copy to .env (git-ignored). Backend reads this file on start.
ANTHROPIC_API_KEY=sk-ant-...
# INSTILENS_JWT_SECRET=change-me-in-production
# INSTILENS_DATABASE_URL=postgresql+psycopg://user:pass@host/instilens
