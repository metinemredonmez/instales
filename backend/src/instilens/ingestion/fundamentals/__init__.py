"""Fundamentals providers: reported statements + trailing metrics behind `resolve_provider()`.

The loader that writes `fundamentals` / `fundamental_snapshots` rows lives in services/fundamentals.py; the CLI,
scheduler and API call that and never a vendor module.
"""
